---
mcpServers:
  osc-mcp:
    type: "remote"
    url: "http://localhost:8666"
---
Check the source package or bundle ${PKG} for the project ${PROJECT} and the repository ${REPO} remotely. 
The project for openSUSE_Tumbleweed is openSUSE:Factory with default as repository.
Check the build log for the ${PKG} and for the project ${PROJECT} and the repository ${REPO} remotely.
For missing files or components try to search for the missing package and check name.
Only report them if they are missing as BuildRequires the spec file and are found for the path openSUSE:Tumbleweed in the standard path.
Analyze the build failure summarize it to a short report.
